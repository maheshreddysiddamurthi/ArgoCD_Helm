apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxfe.fullname" . }}-test-connection"
  labels:
    {{- include "nginxfe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxfe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
